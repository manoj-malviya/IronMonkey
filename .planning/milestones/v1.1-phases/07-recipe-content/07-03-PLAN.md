---
phase: 07-recipe-content
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - IronMonkey.Tests/Integration/EducationRecipeProvisioningTests.cs
autonomous: true
requirements:
  - RCNT-02
  - RCNT-03
must_haves:
  truths:
    - "After provisioning with the Education recipe, the tenant database contains exactly 6 pipeline stages with correct names and StageType values"
    - "After provisioning with the Education recipe, the tenant database contains exactly 6 custom field definitions with correct FieldName and FieldType values"
    - "After provisioning with the Education recipe, the tenant database contains exactly 2 workflow rules with correct names and trigger types"
    - "After provisioning with the Education recipe, the tenant database contains exactly 3 named domain roles (or documented as informational)"
    - "After provisioning with the Education recipe, the tenant database contains exactly 5 sample leads distributed as 2 in Inquiry, 1 in Application, 1 in Under Review, 1 in Interview"
    - "Sample leads have custom field values populated (Program of Interest, Grade/Year Level)"
  artifacts:
    - path: "IronMonkey.Tests/Integration/EducationRecipeProvisioningTests.cs"
      provides: "Integration tests verifying Education recipe provisioning creates correct tenant data"
      contains: "EducationRecipeProvisioningTests"
  key_links:
    - from: "EducationRecipeProvisioningTests"
      to: "TenantProvisioningService.ProvisionTenantAsync"
      via: "Fixed GUID 00000000-0000-0000-0000-000000000003 loaded from migrated central DB"
      pattern: "EducationRecipeId.*00000000-0000-0000-0000-000000000003"
---

<objective>
Write integration tests verifying that provisioning with the Educational Institution recipe (GUID ...003) creates all expected entities — stages, fields, rules, roles, and sample leads — in the tenant database.

Purpose: Provides automated verification for RCNT-02 (Education recipe content) and RCNT-03 (sample leads). Mirrors the Automobile tests but with education-specific content (admissions funnel, student fields, 7-day stale rule).

Output: EducationRecipeProvisioningTests.cs with 6 test methods covering all recipe content assertions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-recipe-content/07-CONTEXT.md
@.planning/phases/07-recipe-content/07-01-SUMMARY.md

<interfaces>
<!-- Mirror of AutomobileRecipeProvisioningTests structure. Education-specific differences noted. -->

Education recipe fixed GUID: 00000000-0000-0000-0000-000000000003
Education stages (6 per D-06): Inquiry [Entry], Application [Active], Under Review [Active], Interview [Active], Enrolled [ClosedWon], Declined [ClosedLost]
Education fields (6 per D-07): Program of Interest [Dropdown], Grade/Year Level [Dropdown], Previous School [Text], Guardian Name [Text], Guardian Phone [Text], Scholarship Needed [Boolean]
Education workflow rules (2 per D-08): "Notify on Review" (StatusChange), "Flag Stale Inquiry" (TimeElapsed, 7 days = 168 hours)
Education roles (3 per D-09): Admissions Director, Admissions Officer, Academic Counselor
Sample lead distribution (per D-11): 2 in Inquiry, 1 in Application, 1 in Under Review, 1 in Interview, 0 in Enrolled, 0 in Declined
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Education recipe content tests (stages, fields, rules, roles)</name>
  <files>IronMonkey.Tests/Integration/EducationRecipeProvisioningTests.cs</files>
  <read_first>
    - IronMonkey.Tests/Integration/AutomobileRecipeProvisioningTests.cs (exact structure to mirror — created in Plan 02)
    - IronMonkey.Tests/Integration/BlankRecipeProvisioningTests.cs (helper method pattern)
    - .planning/phases/07-recipe-content/07-CONTEXT.md (D-06 through D-09: exact stage names, field names, rule names, role names)
    - .planning/phases/07-recipe-content/07-01-SUMMARY.md (confirm migration is present)
  </read_first>
  <action>
    Create IronMonkey.Tests/Integration/EducationRecipeProvisioningTests.cs.

    Namespace: IronMonkey.Tests.Integration
    Class: EducationRecipeProvisioningTests
    Attributes: [Collection("Integration")], IClassFixture&lt;PostgreSqlFixture&gt;
    Fixed GUID: private static readonly Guid EducationRecipeId = new Guid("00000000-0000-0000-0000-000000000003");

    Copy CreateCentralDbContext() and CreateProvisioningService() helper methods verbatim from AutomobileRecipeProvisioningTests.cs (same pattern).

    Write these 4 test methods:

    **Test SeedsAllPipelineStages:**
    - Provision with EducationRecipeId
    - Assert stages.Count == 6
    - Assert contains: "Inquiry" (Entry), "Application" (Active), "Under Review" (Active), "Interview" (Active), "Enrolled" (ClosedWon), "Declined" (ClosedLost)
    - Assert stages ordered by Order property (0..5)
    - Note: "Under Review" contains a space — assert exact string match

    **Test SeedsAllCustomFields:**
    - Provision with EducationRecipeId
    - Assert fields.Count == 6
    - Assert contains "Program of Interest" (FieldType == CustomFieldType.Dropdown)
    - Assert contains "Grade/Year Level" (FieldType == CustomFieldType.Dropdown)
    - Assert contains "Previous School" (FieldType == CustomFieldType.Text)
    - Assert contains "Guardian Name" (FieldType == CustomFieldType.Text)
    - Assert contains "Guardian Phone" (FieldType == CustomFieldType.Text)
    - Assert contains "Scholarship Needed" (FieldType == CustomFieldType.Boolean)

    **Test SeedsWorkflowRules:**
    - Provision with EducationRecipeId
    - Assert rules.Count == 2
    - Assert contains "Notify on Review" (Trigger == WorkflowTrigger.StatusChange)
    - Assert contains "Flag Stale Inquiry" (Trigger == WorkflowTrigger.TimeElapsed)

    **Test SeedsRoles:**
    - Provision with EducationRecipeId
    - Load all roles
    - Same pattern as AutomobileRecipeProvisioningTests.SeedsRoles — verify actual role creation behavior and document with TODO if domain roles are informational only in Phase 7 (per Pitfall 5 in RESEARCH.md)

    Use unique company names per test (Guid.NewGuid().ToString("N")[..8] pattern).

    Required usings (same as AutomobileRecipeProvisioningTests.cs):
    ```csharp
    using IronMonkey.ApiService.Authentication.Services;
    using IronMonkey.Data;
    using IronMonkey.Data.Entities;
    using IronMonkey.Tests.Fixtures;
    using Microsoft.EntityFrameworkCore;
    using Microsoft.Extensions.Configuration;
    using Xunit;
    using BC = BCrypt.Net.BCrypt;
    ```
  </action>
  <verify>
    <automated>dotnet test IronMonkey.Tests --filter "FullyQualifiedName~EducationRecipeProvisioningTests.SeedsAllStages" -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File IronMonkey.Tests/Integration/EducationRecipeProvisioningTests.cs exists
    - grep -n "EducationRecipeId" IronMonkey.Tests/Integration/EducationRecipeProvisioningTests.cs returns the static field with GUID 00000000-0000-0000-0000-000000000003
    - grep -n "Under Review" IronMonkey.Tests/Integration/EducationRecipeProvisioningTests.cs returns the stage name assertion (exact string with space)
    - grep -n "Grade/Year Level" IronMonkey.Tests/Integration/EducationRecipeProvisioningTests.cs returns the field name assertion (exact string with slash)
    - dotnet test IronMonkey.Tests --filter "FullyQualifiedName~EducationRecipeProvisioningTests.SeedsAllPipelineStages" -x exits 0 (test passes)
    - dotnet test IronMonkey.Tests --filter "FullyQualifiedName~EducationRecipeProvisioningTests.SeedsAllCustomFields" -x exits 0 (test passes)
  </acceptance_criteria>
  <done>4 test methods written. SeedsAllPipelineStages and SeedsAllCustomFields pass against migrated Education recipe data. "Under Review" and "Grade/Year Level" stage/field names with special characters pass exact string assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Write Education sample lead tests (RCNT-03)</name>
  <files>IronMonkey.Tests/Integration/EducationRecipeProvisioningTests.cs</files>
  <read_first>
    - IronMonkey.Tests/Integration/EducationRecipeProvisioningTests.cs (after Task 1 — append to this file)
    - IronMonkey.Tests/Integration/AutomobileRecipeProvisioningTests.cs (SeedsSampleLeads and SampleLeadCustomFields tests — mirror with education-specific values)
    - .planning/phases/07-recipe-content/07-CONTEXT.md (D-11: 2 Inquiry, 1 Application, 1 Under Review, 1 Interview; D-13: custom field values)
  </read_first>
  <action>
    Append 2 more test methods to EducationRecipeProvisioningTests.cs:

    **Test SeedsSampleLeads:**
    - Provision with EducationRecipeId
    - Load leads: var leads = await tenantDb.Leads.Include(l => l.Stage).ToListAsync()
    - Assert leads.Count == 5
    - Assert leads.Count(l => l.Stage.Name == "Inquiry") == 2
    - Assert leads.Count(l => l.Stage.Name == "Application") == 1
    - Assert leads.Count(l => l.Stage.Name == "Under Review") == 1
    - Assert leads.Count(l => l.Stage.Name == "Interview") == 1
    - Assert leads.Count(l => l.Stage.Name == "Enrolled") == 0 (no leads in terminal stages)
    - Assert leads.Count(l => l.Stage.Name == "Declined") == 0
    - Assert all leads have Source == LeadSource.WebForm (per D-14)

    **Test SampleLeadCustomFields:**
    - Provision with EducationRecipeId
    - Load leads with stages included
    - Find a lead in "Inquiry" stage: var inquiryLead = leads.First(l => l.Stage.Name == "Inquiry")
    - Assert inquiryLead.CustomFields.Values.ContainsKey("Program of Interest")
    - Assert inquiryLead.CustomFields.Values["Program of Interest"] is not null
    - Find the lead in "Application": var applicationLead = leads.Single(l => l.Stage.Name == "Application")
    - Assert applicationLead.CustomFields.Values.ContainsKey("Program of Interest")
    - Assert applicationLead.CustomFields.Values.ContainsKey("Previous School")
    - Find the lead in "Under Review": var reviewLead = leads.Single(l => l.Stage.Name == "Under Review")
    - Assert reviewLead.CustomFields.Values.ContainsKey("Program of Interest")
  </action>
  <verify>
    <automated>dotnet test IronMonkey.Tests --filter "FullyQualifiedName~EducationRecipeProvisioningTests" -x 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "SeedsSampleLeads\|SampleLeadCustomFields" IronMonkey.Tests/Integration/EducationRecipeProvisioningTests.cs returns 2 additional test methods
    - grep -n "leads.Count == 5" IronMonkey.Tests/Integration/EducationRecipeProvisioningTests.cs returns the assertion line
    - grep -n "Under Review" IronMonkey.Tests/Integration/EducationRecipeProvisioningTests.cs returns at least 2 lines (stage assertion + lead count assertion)
    - dotnet test IronMonkey.Tests --filter "FullyQualifiedName~EducationRecipeProvisioningTests" -x exits 0 (all 6 tests pass)
    - Test output shows 6 passed tests
  </acceptance_criteria>
  <done>All 6 EducationRecipeProvisioningTests pass. Sample lead distribution (2 Inquiry, 1 Application, 1 Under Review, 1 Interview) is verified. Custom field values (Program of Interest, Previous School) are present on leads.</done>
</task>

</tasks>

<verification>
`dotnet test IronMonkey.Tests --filter "FullyQualifiedName~EducationRecipeProvisioningTests" -x` — all 6 tests pass.
`dotnet test IronMonkey.Tests -x` — full test suite passes (all phases, no regressions).
</verification>

<success_criteria>
- EducationRecipeProvisioningTests.cs exists with 6 test methods
- All 6 tests pass against the real PostgreSQL container and migrated recipe data
- Tests verify stage names (including "Under Review" with space), field names (including "Grade/Year Level" with slash), workflow rule triggers, and sample lead distribution (2+1+1+1 across stages)
- Full test suite passes: `dotnet test IronMonkey.Tests -x` exits 0
- Phase 7 requirements RCNT-01, RCNT-02, RCNT-03 all have green automated tests
</success_criteria>

<output>
After completion, create `.planning/phases/07-recipe-content/07-03-SUMMARY.md`
</output>
