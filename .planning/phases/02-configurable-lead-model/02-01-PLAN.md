---
phase: 02-configurable-lead-model
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - IronMonkey.Tests/Integration/CustomFieldTests.cs
  - IronMonkey.Tests/Integration/PipelineStageTests.cs
  - IronMonkey.Tests/Integration/LeadSourceTests.cs
  - IronMonkey.Tests/Integration/DuplicateDetectionTests.cs
  - IronMonkey.Tests/Integration/LeadMergeTests.cs
autonomous: true
requirements: [LEAD-01, LEAD-02, LEAD-03, LEAD-04, LEAD-05]

must_haves:
  truths:
    - "All 5 integration test files exist with named stubs using Fact(Skip=...) pattern"
    - "`dotnet test --filter Phase2` runs without compile errors and reports all stubs as skipped"
    - "Each test class is decorated with [Collection(\"Integration\")] and references the PostgreSqlFixture"
  artifacts:
    - path: "IronMonkey.Tests/Integration/CustomFieldTests.cs"
      provides: "LEAD-01 test stubs (create/list/validate custom fields)"
    - path: "IronMonkey.Tests/Integration/PipelineStageTests.cs"
      provides: "LEAD-02 test stubs (create/list/reorder stages)"
    - path: "IronMonkey.Tests/Integration/LeadSourceTests.cs"
      provides: "LEAD-03 test stubs (lead source enum on create/detail)"
    - path: "IronMonkey.Tests/Integration/DuplicateDetectionTests.cs"
      provides: "LEAD-04 test stubs (email/phone/name duplicate detection)"
    - path: "IronMonkey.Tests/Integration/LeadMergeTests.cs"
      provides: "LEAD-05 test stubs (merge + history preservation)"
  key_links:
    - from: "IronMonkey.Tests/Integration/CustomFieldTests.cs"
      to: "IronMonkey.Tests/Fixtures/PostgreSqlFixture.cs"
      via: "[Collection(\"Integration\")] attribute"
      pattern: "Collection.*Integration"
---

<objective>
Create Wave 0 test scaffolding — 5 integration test stub files covering all Phase 2 requirements.

Purpose: Nyquist compliance requires test files to exist before implementation tasks reference them via `<automated>` verify commands. Stubs use [Fact(Skip="...")] so the test runner reports them as Skipped, not absent. This prevents "file not found" errors when later plans run `dotnet test --filter Phase2`.

Output: 5 stub test files with named test methods mirroring the Phase 2 validation strategy acceptance criteria.
</objective>

<execution_context>
@/home/manoj/.claude/get-shit-done/workflows/execute-plan.md
@/home/manoj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-configurable-lead-model/02-VALIDATION.md

<!-- Established stub pattern from Phase 1 Wave 0 -->
</context>

<interfaces>
<!-- Phase 1 established test patterns — executor MUST follow these exactly. -->

From IronMonkey.Tests/Integration/TenantIsolationTests.cs (established pattern):
```csharp
[Collection("Integration")]
public class TenantIsolationTests(PostgreSqlFixture fixture) : IClassFixture<PostgreSqlFixture>
{
    [Fact(Skip = "Wave 0 stub — implement in plan 01-06")]
    public async Task LeadCreatedForTenantA_IsNotVisibleToTenantB()
    {
        // Arrange
        // Act
        // Assert
        await Task.CompletedTask;
    }
}
```

From IronMonkey.Tests/Fixtures/PostgreSqlFixture.cs:
```csharp
[CollectionDefinition("Integration")]
public class IntegrationTestCollection : ICollectionFixture<PostgreSqlFixture> { }
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create Wave 0 test stubs for LEAD-01, LEAD-02, LEAD-03</name>
  <files>
    IronMonkey.Tests/Integration/CustomFieldTests.cs,
    IronMonkey.Tests/Integration/PipelineStageTests.cs,
    IronMonkey.Tests/Integration/LeadSourceTests.cs
  </files>
  <action>
Create 3 integration test stub files. Each file must:
- Use `[Collection("Integration")]` class attribute
- Implement `IClassFixture&lt;PostgreSqlFixture&gt;` via primary constructor injection
- Use `[Fact(Skip = "Wave 0 stub — implement in plan 02-06")]` on all test methods
- End each test body with `await Task.CompletedTask;`
- Add `using IronMonkey.Tests.Fixtures;` and `using Xunit;`

**CustomFieldTests.cs** — 4 stubs:
1. `CanCreateCustomTextField_ForTenant` — POST custom field def returns 201
2. `CanCreateCustomDropdownField_WithOptions` — dropdown field with options stored
3. `ListCustomFields_ReturnsOnlyTenantFields` — isolation check
4. `CustomFieldValue_IsValidatedAgainstType` — type enforcement (number field rejects text)

**PipelineStageTests.cs** — 3 stubs:
1. `CanCreatePipelineStage_WithNameAndOrder` — POST stage returns 201
2. `ListPipelineStages_ReturnsOnlyActiveStages` — IsActive filter works
3. `LeadStageId_MustReferenceValidTenantStage` — FK enforcement

**LeadSourceTests.cs** — 2 stubs:
1. `CreateLead_WithManualSource_SourceStoredCorrectly` — Manual enum value on create
2. `LeadDetail_IncludesSourceField` — source visible in GET /leads/{id}
  </action>
  <verify>
    <automated>dotnet test IronMonkey.Tests/IronMonkey.Tests.csproj --filter "FullyQualifiedName~CustomField|FullyQualifiedName~PipelineStage|FullyQualifiedName~LeadSource" --no-build 2>/dev/null; dotnet build IronMonkey.Tests/IronMonkey.Tests.csproj -q</automated>
  </verify>
  <done>3 test files compile cleanly; `dotnet test --filter` reports all 9 stubs as Skipped</done>
</task>

<task type="auto">
  <name>Task 2: Create Wave 0 test stubs for LEAD-04 and LEAD-05</name>
  <files>
    IronMonkey.Tests/Integration/DuplicateDetectionTests.cs,
    IronMonkey.Tests/Integration/LeadMergeTests.cs
  </files>
  <action>
Create 2 integration test stub files following the same pattern as Task 1.

**DuplicateDetectionTests.cs** — 4 stubs:
1. `DuplicateCheck_OnMatchingEmail_ReturnsCandidates` — exact email match surfaces warning
2. `DuplicateCheck_OnMatchingPhone_ReturnsCandidates` — exact phone match surfaces warning
3. `DuplicateCheck_OnSimilarName_ReturnsFuzzyMatch` — fuzzy name match at 75%+ threshold
4. `DuplicateCheck_WithNoMatches_ReturnsEmptyList` — no false positives

**LeadMergeTests.cs** — 4 stubs:
1. `MergeLeads_SurvivingLeadRetainsSourceFields` — source lead fields preserved
2. `MergeLeads_TargetLeadIsSoftDeleted` — target marked IsDeleted
3. `MergeLeads_AuditRecordCreated_WithBothLeadIds` — LeadMerge entity persisted
4. `MergeLeads_SurvivingLeadCustomFieldsIncludeTargetValues` — custom field merge logic
  </action>
  <verify>
    <automated>dotnet build IronMonkey.Tests/IronMonkey.Tests.csproj -q && dotnet test IronMonkey.Tests/IronMonkey.Tests.csproj --filter "FullyQualifiedName~DuplicateDetection|FullyQualifiedName~LeadMerge"</automated>
  </verify>
  <done>2 test files compile cleanly; all 8 stubs reported as Skipped; `dotnet test --filter Phase2` shows 17 total skipped tests</done>
</task>

</tasks>

<verification>
Run full Phase 2 filter after both tasks:
`dotnet test IronMonkey.Tests/IronMonkey.Tests.csproj --filter "FullyQualifiedName~CustomField|FullyQualifiedName~PipelineStage|FullyQualifiedName~LeadSource|FullyQualifiedName~DuplicateDetection|FullyQualifiedName~LeadMerge"`

Expected: 17 tests, all Skipped, 0 failures, 0 errors.
</verification>

<success_criteria>
- All 5 test files exist on disk
- `dotnet build IronMonkey.Tests` passes with no errors
- All 17 stub tests appear in test runner output as Skipped
- No test body throws or fails — bodies only contain `await Task.CompletedTask;`
</success_criteria>

<output>
After completion, create `.planning/phases/02-configurable-lead-model/02-01-SUMMARY.md`
</output>
