---
phase: 02-configurable-lead-model
plan: "05"
type: execute
wave: 4
depends_on: ["02-03", "02-04"]
files_modified:
  - IronMonkey.Tests/Integration/CustomFieldTests.cs
  - IronMonkey.Tests/Integration/PipelineStageTests.cs
  - IronMonkey.Tests/Integration/LeadSourceTests.cs
  - IronMonkey.Tests/Integration/DuplicateDetectionTests.cs
  - IronMonkey.Tests/Integration/LeadMergeTests.cs
  - IronMonkey.Tests/Fixtures/PostgreSqlFixture.cs
autonomous: false
requirements: [LEAD-01, LEAD-02, LEAD-03, LEAD-04, LEAD-05]

must_haves:
  truths:
    - "All 17 Phase 2 integration tests pass (no skips, no failures)"
    - "Each test provisions a real PostgreSQL tenant database via TestContainers"
    - "Tests verify actual database state after each API operation"
    - "Custom field type validation is tested for all 7 field types"
    - "Duplicate detection tests cover email exact match, phone exact match, and name fuzzy match"
    - "Merge test verifies soft-delete of target and LeadMerge audit record in database"
  artifacts:
    - path: "IronMonkey.Tests/Integration/CustomFieldTests.cs"
      provides: "4 passing tests for LEAD-01"
      exports: ["CanCreateCustomTextField_ForTenant", "CanCreateCustomDropdownField_WithOptions", "ListCustomFields_ReturnsOnlyTenantFields", "CustomFieldValue_IsValidatedAgainstType"]
    - path: "IronMonkey.Tests/Integration/PipelineStageTests.cs"
      provides: "3 passing tests for LEAD-02"
    - path: "IronMonkey.Tests/Integration/LeadSourceTests.cs"
      provides: "2 passing tests for LEAD-03"
    - path: "IronMonkey.Tests/Integration/DuplicateDetectionTests.cs"
      provides: "4 passing tests for LEAD-04"
    - path: "IronMonkey.Tests/Integration/LeadMergeTests.cs"
      provides: "4 passing tests for LEAD-05"
  key_links:
    - from: "IronMonkey.Tests/Integration/CustomFieldTests.cs"
      to: "IronMonkey.Data/TenantDbContext.cs"
      via: "TenantDbContext with TestContainers PostgreSQL connection"
      pattern: "PostgreSqlFixture|TenantDbContext"
    - from: "IronMonkey.Tests/Integration/LeadMergeTests.cs"
      to: "IronMonkey.ApiService/Features/Leads/Merge/LeadMergeService.cs"
      via: "Direct service invocation with real TenantDbContext"
      pattern: "LeadMergeService|MergeAsync"
---

<objective>
Implement all 17 integration tests replacing Wave 0 stubs with passing tests.

Purpose: Provides the automated verification backbone for Phase 2. Every requirement (LEAD-01 through LEAD-05) gets end-to-end database verification with real PostgreSQL via TestContainers.

Output: 17 passing integration tests that can be re-run as regression suite for Phase 3+.
</objective>

<execution_context>
@/home/manoj/.claude/get-shit-done/workflows/execute-plan.md
@/home/manoj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-configurable-lead-model/02-03-SUMMARY.md
@.planning/phases/02-configurable-lead-model/02-04-SUMMARY.md
@IronMonkey.Tests/Fixtures/PostgreSqlFixture.cs
@IronMonkey.Tests/Integration/TenantIsolationTests.cs
@IronMonkey.Data/TenantDbContext.cs
@IronMonkey.Data/Entities/Lead.cs
@IronMonkey.Data/Entities/CustomFieldDefinition.cs
@IronMonkey.Data/Entities/PipelineStage.cs
@IronMonkey.Data/Entities/LeadMerge.cs
</context>

<interfaces>
<!-- Phase 1 test fixture patterns — must be preserved -->

From IronMonkey.Tests/Fixtures/PostgreSqlFixture.cs (Phase 1 established):
```csharp
[CollectionDefinition("Integration")]
public class IntegrationTestCollection : ICollectionFixture<PostgreSqlFixture> { }

public class PostgreSqlFixture : IAsyncLifetime
{
    private PostgreSqlContainer _container;
    public string ConnectionString { get; private set; }
    // Creates a fresh PostgreSQL container per collection
}
```

Phase 1 test class pattern:
```csharp
[Collection("Integration")]
public class SomeTests(PostgreSqlFixture fixture) : IClassFixture<PostgreSqlFixture>
{
    [Fact]
    public async Task SomeBehavior()
    {
        // Use fixture.ConnectionString to build TenantDbContext
    }
}
```

Test setup pattern for tenant database (from Phase 1 TenantIsolationTests):
- Build TenantDbContext with fixture.ConnectionString + a tenant Guid
- Call MigrateAsync() to apply all migrations to the test database
- Seed minimal data (PipelineStage) before creating leads
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Implement CustomField, PipelineStage, and LeadSource tests</name>
  <files>
    IronMonkey.Tests/Integration/CustomFieldTests.cs,
    IronMonkey.Tests/Integration/PipelineStageTests.cs,
    IronMonkey.Tests/Integration/LeadSourceTests.cs
  </files>
  <action>
Replace all `[Fact(Skip=...)]` stubs with full `[Fact]` implementations. Each test must:
1. Build a `TenantDbContext` pointing at `fixture.ConnectionString` with a unique `Guid.NewGuid()` tenantId
2. Call `await db.Database.MigrateAsync()` to create schema
3. Arrange, Act via direct service/entity calls or HTTP client, Assert via db queries

**CustomFieldTests.cs** — 4 tests:

`CanCreateCustomTextField_ForTenant`:
- Create CustomFieldDefinition (Text, not required), SaveChangesAsync
- Query back from db, assert FieldName matches, FieldType == Text

`CanCreateCustomDropdownField_WithOptions`:
- Create Dropdown field with options ["Option A", "Option B"], SaveChangesAsync
- Query back, assert Options.Count == 2 and contains "Option A"

`ListCustomFields_ReturnsOnlyTenantFields`:
- Create 2 tenants with separate TenantDbContexts using same PostgreSQL but different tenantIds
- Add 2 fields to tenant A, 1 field to tenant B
- Query tenant A context: assert 2 results; query tenant B context: assert 1 result

`CustomFieldValue_IsValidatedAgainstType`:
- Create a PipelineStage, create a Lead
- Create a Number field definition
- Try setting a text value via CustomFieldValues.Set() and verify via a unit assertion or service validation
  (Note: JSONB itself is flexible; this test validates that the custom field definition type is enforced at the service layer, not at DB level — test the validation logic in CreateLeadEndpoint or a validator, not DB constraints)

**PipelineStageTests.cs** — 3 tests:

`CanCreatePipelineStage_WithNameAndOrder`:
- Create PipelineStage("Qualified", 1), SaveChangesAsync
- Query back, assert Name == "Qualified", Order == 1, IsActive == true

`ListPipelineStages_ReturnsOnlyActiveStages`:
- Create 2 stages: one active, one deactivated (Deactivate())
- Query db.PipelineStages.ToListAsync() — global filter enforces IsActive
- Assert only 1 result returned

`LeadStageId_MustReferenceValidTenantStage`:
- Create a Lead with a Guid.NewGuid() stageId that has no matching PipelineStage
- Assert SaveChangesAsync throws DbUpdateException (FK violation)

**LeadSourceTests.cs** — 2 tests:

`CreateLead_WithManualSource_SourceStoredCorrectly`:
- Seed a PipelineStage, create Lead.Create(..., LeadSource.Manual, stageId)
- SaveChangesAsync, query back, assert Source == LeadSource.Manual

`LeadDetail_IncludesSourceField`:
- Create a lead with LeadSource.Import
- Query via db.Leads.FirstAsync(), assert Source == LeadSource.Import
  </action>
  <verify>
    <automated>dotnet test IronMonkey.Tests/IronMonkey.Tests.csproj --filter "FullyQualifiedName~CustomField|FullyQualifiedName~PipelineStage|FullyQualifiedName~LeadSource" -v minimal 2>&1 | tail -15</automated>
  </verify>
  <done>9 tests run and pass (no skips); LeadStageId FK test correctly catches DbUpdateException</done>
</task>

<task type="auto">
  <name>Task 2: Implement DuplicateDetection and LeadMerge tests</name>
  <files>
    IronMonkey.Tests/Integration/DuplicateDetectionTests.cs,
    IronMonkey.Tests/Integration/LeadMergeTests.cs
  </files>
  <action>
Replace all `[Fact(Skip=...)]` stubs with full `[Fact]` implementations.

**DuplicateDetectionTests.cs** — 4 tests. Each test:
- Creates a TenantDbContext, runs MigrateAsync
- Seeds a PipelineStage (required FK for leads)
- Seeds 1-2 leads with specific email/phone/name
- Instantiates DuplicateDetectionService directly with a mock or stub ITenantService that returns the test TenantDbContext
- Calls FindCandidatesAsync and asserts results

`DuplicateCheck_OnMatchingEmail_ReturnsCandidates`:
- Seed lead with email="john@example.com"
- Call FindCandidatesAsync(tenantId, email:"john@example.com", phone:null, name:null)
- Assert candidates.Count == 1, candidates[0].ConfidenceScore == 100

`DuplicateCheck_OnMatchingPhone_ReturnsCandidates`:
- Seed lead with Mobile="555-123-4567"
- Call FindCandidatesAsync(tenantId, email:null, phone:"5551234567", name:null) — normalized form
- Assert candidates.Count == 1, candidates[0].ConfidenceScore == 95

`DuplicateCheck_OnSimilarName_ReturnsFuzzyMatch`:
- Seed lead with FirstName="John" LastName="Smith"
- Call FindCandidatesAsync(tenantId, email:null, phone:null, name:"Jon Smith")
- Assert candidates.Count >= 1 (fuzzy match should score 75%+ for "Jon Smith" vs "John Smith")

`DuplicateCheck_WithNoMatches_ReturnsEmptyList`:
- Seed lead with email="other@example.com"
- Call FindCandidatesAsync(tenantId, email:"nomatch@example.com", phone:null, name:null)
- Assert candidates.Count == 0

**LeadMergeTests.cs** — 4 tests. Each test:
- Creates TenantDbContext, runs MigrateAsync
- Seeds a PipelineStage, creates 2 leads
- Instantiates LeadMergeService with test TenantDbContext (inject via ITenantService stub)
- Calls MergeAsync and asserts database state

`MergeLeads_SurvivingLeadRetainsSourceFields`:
- Create source lead with FirstName="Alice", target lead with FirstName="Bob"
- MergeAsync(tenantId, sourceId, targetId, userId)
- Query source lead, assert FirstName == "Alice" (source retained)

`MergeLeads_TargetLeadIsSoftDeleted`:
- Create 2 leads, call MergeAsync
- Query target lead with IgnoreQueryFilters(), assert IsDeleted == true and DeletedAt != null

`MergeLeads_AuditRecordCreated_WithBothLeadIds`:
- Create 2 leads, call MergeAsync
- Query db.LeadMerges, assert 1 record with matching SourceLeadId and TargetLeadId

`MergeLeads_SurvivingLeadCustomFieldsIncludeTargetValues`:
- Create a CustomFieldDefinition with id "field-1"
- Create source lead with CustomFields.Values["field-1"] = null
- Create target lead with CustomFields.Values["field-1"] = "target-value"
- MergeAsync — source's null field should be replaced by target's "target-value"
- Query source lead, assert CustomFields.Values["field-1"] == "target-value"
  </action>
  <verify>
    <automated>dotnet test IronMonkey.Tests/IronMonkey.Tests.csproj --filter "FullyQualifiedName~DuplicateDetection|FullyQualifiedName~LeadMerge" -v minimal 2>&1 | tail -15</automated>
  </verify>
  <done>8 tests run and pass; merge audit record verified in database; fuzzy match test confirms FuzzySharp scoring</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 2 complete: custom fields (LEAD-01), pipeline stages (LEAD-02), lead source tracking (LEAD-03), duplicate detection (LEAD-04), and lead merge with audit (LEAD-05). All 17 integration tests passing, full solution building clean.
  </what-built>
  <how-to-verify>
    1. Run full test suite: `dotnet test IronMonkey.Tests/IronMonkey.Tests.csproj`
       Expected: all Phase 2 tests pass, Phase 1 tests still pass, no failures.

    2. Run Phase 2 filter: `dotnet test IronMonkey.Tests/IronMonkey.Tests.csproj --filter "FullyQualifiedName~CustomField|FullyQualifiedName~PipelineStage|FullyQualifiedName~LeadSource|FullyQualifiedName~DuplicateDetection|FullyQualifiedName~LeadMerge"`
       Expected: 17 passed, 0 skipped, 0 failed.

    3. Verify build: `dotnet build IronMonkey.sln -q`
       Expected: 0 errors, 0 warnings.

    4. Spot-check the duplicate detection: `grep -r "TokenSetRatio" IronMonkey.ApiService/Features/Leads/Duplicates/DuplicateDetectionService.cs`
       Expected: FuzzySharp.Fuzz.TokenSetRatio used with threshold 75.

    5. Spot-check the merge audit: `grep -r "LeadMerge.Create\|SourceSnapshot\|TargetSnapshot" IronMonkey.ApiService/Features/Leads/Merge/LeadMergeService.cs`
       Expected: both snapshot fields populated before SaveChangesAsync.
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any failures to address</resume-signal>
</task>

</tasks>

<verification>
Full phase verification:
`dotnet test IronMonkey.Tests/IronMonkey.Tests.csproj` — all tests (Phase 1 + Phase 2) green.
`dotnet build IronMonkey.sln -q` — zero errors.
</verification>

<success_criteria>
- All 17 Phase 2 integration tests pass (no skips)
- All Phase 1 tests still pass (regression check)
- Tests verify actual database state via direct TenantDbContext queries
- Fuzzy name matching test confirms FuzzySharp scores "Jon Smith" vs "John Smith" above 75%
- Merge test confirms LeadMerge audit record persisted with both lead IDs
- Human verification checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/02-configurable-lead-model/02-05-SUMMARY.md`
</output>
