---
phase: 04-pipeline-workflow-engine
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - IronMonkey.Tests/Integration/KanbanBoardTests.cs
  - IronMonkey.Tests/Integration/TaskTests.cs
  - IronMonkey.Tests/Integration/LeadRoutingTests.cs
  - IronMonkey.Tests/Integration/WorkflowRuleTests.cs
  - IronMonkey.Tests/Integration/StateTransitionTests.cs
  - IronMonkey.Tests/Unit/StageTransitionConfigurationTests.cs
  - IronMonkey.Tests/IronMonkey.Tests.csproj
  - IronMonkey.ApiService/IronMonkey.ApiService.csproj
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
  - PIPE-03
  - PIPE-04
  - PIPE-05

must_haves:
  truths:
    - "All 6 test files exist with [Fact(Skip)] stubs for Phase 4 behaviors"
    - "Test project references Stateless 5.20.1 and RulesEngine 5.1.2"
    - "ApiService project references Stateless 5.20.1 and RulesEngine 5.1.2"
    - "Running the test filter command exits 0 (all stubs skipped, none failing)"
  artifacts:
    - path: "IronMonkey.Tests/Integration/KanbanBoardTests.cs"
      provides: "PIPE-01 test stubs"
    - path: "IronMonkey.Tests/Integration/TaskTests.cs"
      provides: "PIPE-02 test stubs"
    - path: "IronMonkey.Tests/Integration/LeadRoutingTests.cs"
      provides: "PIPE-03 test stubs"
    - path: "IronMonkey.Tests/Integration/WorkflowRuleTests.cs"
      provides: "PIPE-04 test stubs"
    - path: "IronMonkey.Tests/Integration/StateTransitionTests.cs"
      provides: "PIPE-05 integration test stubs"
    - path: "IronMonkey.Tests/Unit/StageTransitionConfigurationTests.cs"
      provides: "PIPE-05 unit test stubs"
  key_links:
    - from: "IronMonkey.Tests/Integration/KanbanBoardTests.cs"
      to: "PostgreSqlFixture"
      via: "IClassFixture<PostgreSqlFixture>"
      pattern: "IClassFixture<PostgreSqlFixture>"
---

<objective>
Wave 0: Create test stubs for all Phase 4 behaviors. Every test is [Fact(Skip)] with a clear description of what it will eventually assert. Add Stateless and RulesEngine package references so later plans can use them immediately.

Purpose: Nyquist compliance — every implementation plan has a test file waiting. Establishes RED state before any GREEN.
Output: 6 test files with [Fact(Skip)] stubs + updated .csproj files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-pipeline-workflow-engine/04-CONTEXT.md
@.planning/phases/04-pipeline-workflow-engine/04-VALIDATION.md

<interfaces>
<!-- Existing test pattern — replicate exactly -->
From IronMonkey.Tests/Integration/PipelineStageTests.cs:
```csharp
[Collection("Integration")]
public class PipelineStageTests(PostgreSqlFixture fixture) : IClassFixture<PostgreSqlFixture>
{
    private readonly TenantDbContextFactory _factory = new();
    // ...
    [Fact]
    public async Task CanCreatePipelineStage_WithNameAndOrder() { ... }
}
```

From IronMonkey.Tests/Integration/DuplicateDetectionTests.cs (service pattern):
```csharp
[Collection("Integration")]
public class DuplicateDetectionTests(PostgreSqlFixture fixture) : IClassFixture<PostgreSqlFixture>
{
    private readonly TenantDbContextFactory _factory = new();
    private async Task<(string connStr, Guid stageId)> SetupDbAsync(Guid tenantId) { ... }
    private DuplicateDetectionService CreateService(Guid tenantId, string connStr) { ... }
}
```

Stub pattern (used across phases 1-3):
```csharp
[Fact(Skip = "Stub: implement in Phase 4")]
public async Task MethodName_WhenCondition_ExpectedBehavior()
{
    // TODO: implement
    await Task.CompletedTask;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Stateless and RulesEngine packages</name>
  <files>
    IronMonkey.ApiService/IronMonkey.ApiService.csproj
    IronMonkey.Tests/IronMonkey.Tests.csproj
  </files>
  <read_first>
    - IronMonkey.ApiService/IronMonkey.ApiService.csproj
    - IronMonkey.Tests/IronMonkey.Tests.csproj
  </read_first>
  <action>
Add the following PackageReference entries to IronMonkey.ApiService.csproj inside the existing ItemGroup that contains other PackageReferences:

```xml
<PackageReference Include="Stateless" Version="5.20.1" />
<PackageReference Include="RulesEngine" Version="5.1.2" />
```

Add the same two PackageReference entries to IronMonkey.Tests.csproj inside the existing ItemGroup that contains other PackageReferences:

```xml
<PackageReference Include="Stateless" Version="5.20.1" />
<PackageReference Include="RulesEngine" Version="5.1.2" />
```

After editing both files, run: `dotnet restore IronMonkey.sln`
  </action>
  <verify>
    <automated>grep -r "Stateless" /home/manoj/projects/sandbox/IronMonkey/IronMonkey.ApiService/IronMonkey.ApiService.csproj && grep -r "RulesEngine" /home/manoj/projects/sandbox/IronMonkey/IronMonkey.ApiService/IronMonkey.ApiService.csproj && grep -r "Stateless" /home/manoj/projects/sandbox/IronMonkey/IronMonkey.Tests/IronMonkey.Tests.csproj && grep -r "RulesEngine" /home/manoj/projects/sandbox/IronMonkey/IronMonkey.Tests/IronMonkey.Tests.csproj</automated>
  </verify>
  <done>Both .csproj files contain Stateless 5.20.1 and RulesEngine 5.1.2 references. dotnet restore exits 0.</done>
  <acceptance_criteria>
    - IronMonkey.ApiService.csproj contains `Include="Stateless" Version="5.20.1"`
    - IronMonkey.ApiService.csproj contains `Include="RulesEngine" Version="5.1.2"`
    - IronMonkey.Tests.csproj contains `Include="Stateless" Version="5.20.1"`
    - IronMonkey.Tests.csproj contains `Include="RulesEngine" Version="5.1.2"`
    - `dotnet restore IronMonkey.sln` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create integration test stubs for PIPE-01 through PIPE-05</name>
  <files>
    IronMonkey.Tests/Integration/KanbanBoardTests.cs
    IronMonkey.Tests/Integration/TaskTests.cs
    IronMonkey.Tests/Integration/LeadRoutingTests.cs
    IronMonkey.Tests/Integration/WorkflowRuleTests.cs
    IronMonkey.Tests/Integration/StateTransitionTests.cs
    IronMonkey.Tests/Unit/StageTransitionConfigurationTests.cs
  </files>
  <read_first>
    - IronMonkey.Tests/Integration/PipelineStageTests.cs
    - IronMonkey.Tests/Integration/DuplicateDetectionTests.cs
    - IronMonkey.Tests/IronMonkey.Tests.csproj
    - .planning/phases/04-pipeline-workflow-engine/04-VALIDATION.md
  </read_first>
  <action>
Create each file below. Use `[Fact(Skip = "Stub: implement in 04-06")]` for all test methods. Follow the exact class structure of existing integration tests.

**IronMonkey.Tests/Integration/KanbanBoardTests.cs** — PIPE-01:
```csharp
using IronMonkey.Data;
using IronMonkey.Tests.Fixtures;
using Xunit;

namespace IronMonkey.Tests.Integration;

// PIPE-01: User can view leads in a Kanban-style pipeline board with drag-drop between stages
[Collection("Integration")]
public class KanbanBoardTests(PostgreSqlFixture fixture) : IClassFixture<PostgreSqlFixture>
{
    private readonly TenantDbContextFactory _factory = new();

    [Fact(Skip = "Stub: implement in 04-06")]
    public async Task GetKanbanBoard_ReturnsLeadsGroupedByStage()
    {
        // TODO: Create leads in 2 stages, call GetKanbanBoard, verify each stage column contains correct leads
        await Task.CompletedTask;
    }

    [Fact(Skip = "Stub: implement in 04-06")]
    public async Task MoveLead_ValidTransition_UpdatesStageAndReturns200()
    {
        // TODO: Configure allowed transition A->B, place lead in A, move to B, verify PipelineStageId updated
        await Task.CompletedTask;
    }

    [Fact(Skip = "Stub: implement in 04-06")]
    public async Task MoveLead_InvalidTransition_Returns400WithAllowedStages()
    {
        // TODO: Configure only A->B, attempt A->C, verify 400 with message "Cannot move from [A] to [C]. Allowed: [B]"
        await Task.CompletedTask;
    }

    [Fact(Skip = "Stub: implement in 04-06")]
    public async Task GetKanbanBoard_WithAgentFilter_ReturnsOnlyFilteredLeads()
    {
        // TODO: Assign leads to two agents, filter by agent A, verify only agent A leads returned
        await Task.CompletedTask;
    }

    [Fact(Skip = "Stub: implement in 04-06")]
    public async Task GetKanbanBoard_VirtualScroll_ReturnsFirst20LeadsPerStage()
    {
        // TODO: Create 25 leads in one stage, fetch board, verify column has 20 leads and hasMore=true
        await Task.CompletedTask;
    }
}
```

**IronMonkey.Tests/Integration/TaskTests.cs** — PIPE-02:
```csharp
using IronMonkey.Data;
using IronMonkey.Tests.Fixtures;
using Xunit;

namespace IronMonkey.Tests.Integration;

// PIPE-02: User can create tasks with due dates, priority, and assignment linked to leads
[Collection("Integration")]
public class TaskTests(PostgreSqlFixture fixture) : IClassFixture<PostgreSqlFixture>
{
    private readonly TenantDbContextFactory _factory = new();

    [Fact(Skip = "Stub: implement in 04-06")]
    public async Task CreateTask_LinkedToLead_PersistsWithAllFields()
    {
        // TODO: Create task with Title, DueDate, Priority (Low/Medium/High/Urgent), AssignedToUserId, LeadId
        // Verify all fields persisted correctly
        await Task.CompletedTask;
    }

    [Fact(Skip = "Stub: implement in 04-06")]
    public async Task ListTasksByAssignee_ReturnsOnlyAssigneeTasks()
    {
        // TODO: Create tasks for user A and user B, list by user A, verify only user A tasks returned
        await Task.CompletedTask;
    }

    [Fact(Skip = "Stub: implement in 04-06")]
    public async Task UpdateTask_ChangesStatusAndPriority()
    {
        // TODO: Create task, update Status to Completed and Priority to High, verify changes persisted
        await Task.CompletedTask;
    }

    [Fact(Skip = "Stub: implement in 04-06")]
    public async Task CreateTask_WithoutLeadId_Returns400()
    {
        // TODO: POST to /api/tasks without LeadId, verify 400
        await Task.CompletedTask;
    }
}
```

**IronMonkey.Tests/Integration/LeadRoutingTests.cs** — PIPE-03:
```csharp
using IronMonkey.Data;
using IronMonkey.Tests.Fixtures;
using Xunit;

namespace IronMonkey.Tests.Integration;

// PIPE-03: System supports manual lead assignment and rule-based routing (round-robin, territory)
[Collection("Integration")]
public class LeadRoutingTests(PostgreSqlFixture fixture) : IClassFixture<PostgreSqlFixture>
{
    private readonly TenantDbContextFactory _factory = new();

    [Fact(Skip = "Stub: implement in 04-06")]
    public async Task RoundRobinRouting_AssignsLeadsInOrder_AgentABCThenRepeat()
    {
        // TODO: Configure round-robin with agents [A, B, C], create 4 leads, verify assignments: A, B, C, A
        await Task.CompletedTask;
    }

    [Fact(Skip = "Stub: implement in 04-06")]
    public async Task TerritoryRouting_ByLeadSource_AssignsToMatchingAgent()
    {
        // TODO: Configure territory routing dimension=LeadSource, map ApiSource->Agent A,
        // create API lead, verify AssignedToUserId = Agent A
        await Task.CompletedTask;
    }

    [Fact(Skip = "Stub: implement in 04-06")]
    public async Task Routing_NoMatchingTerritory_LeavesLeadUnassigned()
    {
        // TODO: Configure territory routing with no matching dimension value, verify AssignedToUserId = null
        await Task.CompletedTask;
    }

    [Fact(Skip = "Stub: implement in 04-06")]
    public async Task ManualAssignment_OverridesRoutingConfig()
    {
        // TODO: Round-robin enabled, manually set AssignedToUserId via endpoint, verify manual assignment wins
        await Task.CompletedTask;
    }

    [Fact(Skip = "Stub: implement in 04-06")]
    public async Task ConfigureRouting_PersistsStrategyAndDimension()
    {
        // TODO: POST /api/routing-config, verify RoutingConfig entity persisted with correct Strategy and Dimension
        await Task.CompletedTask;
    }
}
```

**IronMonkey.Tests/Integration/WorkflowRuleTests.cs** — PIPE-04:
```csharp
using IronMonkey.Data;
using IronMonkey.Tests.Fixtures;
using Xunit;

namespace IronMonkey.Tests.Integration;

// PIPE-04: Tenant can define workflow rules: triggers, conditions, and auto-actions
[Collection("Integration")]
public class WorkflowRuleTests(PostgreSqlFixture fixture) : IClassFixture<PostgreSqlFixture>
{
    private readonly TenantDbContextFactory _factory = new();

    [Fact(Skip = "Stub: implement in 04-06")]
    public async Task CreateWorkflowRule_PersistsWithTriggerConditionAndAction()
    {
        // TODO: POST rule with Trigger="status_change", ConditionJson (RulesEngine format),
        // ActionJson={"type":"notify","userId":...}, verify persisted with IsActive=true
        await Task.CompletedTask;
    }

    [Fact(Skip = "Stub: implement in 04-06")]
    public async Task WorkflowRule_StatusChangeTrigger_FiresNotificationAction()
    {
        // TODO: Create rule trigger=status_change -> action=notify, move lead to new stage,
        // run WorkflowRuleEvaluationJob, verify Notification created for target user
        await Task.CompletedTask;
    }

    [Fact(Skip = "Stub: implement in 04-06")]
    public async Task WorkflowRule_IsActive_False_DoesNotFire()
    {
        // TODO: Create disabled rule (IsActive=false), trigger condition, run job,
        // verify no Notification created
        await Task.CompletedTask;
    }

    [Fact(Skip = "Stub: implement in 04-06")]
    public async Task WorkflowRule_TimeElapsedTrigger_FiresViaHangfireJob()
    {
        // TODO: Create rule trigger=time_elapsed (1 hour), manually invoke TimeElapsedRuleScanJob,
        // verify action fires for qualifying leads
        await Task.CompletedTask;
    }

    [Fact(Skip = "Stub: implement in 04-06")]
    public async Task ListWorkflowRules_ReturnsAllRulesWithIsActiveStatus()
    {
        // TODO: Create 2 rules (1 active, 1 inactive), GET /api/workflow-rules,
        // verify both returned with correct IsActive
        await Task.CompletedTask;
    }
}
```

**IronMonkey.Tests/Integration/StateTransitionTests.cs** — PIPE-05:
```csharp
using IronMonkey.Data;
using IronMonkey.Tests.Fixtures;
using Xunit;

namespace IronMonkey.Tests.Integration;

// PIPE-05: Lead lifecycle follows a configurable state machine with allowed transitions per tenant
[Collection("Integration")]
public class StateTransitionTests(PostgreSqlFixture fixture) : IClassFixture<PostgreSqlFixture>
{
    private readonly TenantDbContextFactory _factory = new();

    [Fact(Skip = "Stub: implement in 04-06")]
    public async Task ConfigureTransition_AllowsFromToStage()
    {
        // TODO: POST /api/stage-transitions with FromStageId and ToStageId,
        // verify StageTransition entity persisted in DB
        await Task.CompletedTask;
    }

    [Fact(Skip = "Stub: implement in 04-06")]
    public async Task MoveLead_AllowedTransition_Succeeds()
    {
        // TODO: Configure transition A->B, move lead from A to B via endpoint, verify 200 and new PipelineStageId
        await Task.CompletedTask;
    }

    [Fact(Skip = "Stub: implement in 04-06")]
    public async Task MoveLead_ForbiddenTransition_Returns400WithAllowedList()
    {
        // TODO: Configure only A->B, attempt A->C via endpoint,
        // verify 400 body contains "Allowed: [B stage name]"
        await Task.CompletedTask;
    }

    [Fact(Skip = "Stub: implement in 04-06")]
    public async Task ClosedWon_Stage_ExcludedFromActiveBoardCount()
    {
        // TODO: Create stage with StageType=ClosedWon, place lead in it,
        // verify GetKanbanBoard active count excludes closed stage leads
        await Task.CompletedTask;
    }
}
```

**IronMonkey.Tests/Unit/StageTransitionConfigurationTests.cs** — PIPE-05 unit tests:
```csharp
using Xunit;

namespace IronMonkey.Tests.Unit;

// PIPE-05: State machine graph setup unit tests
public class StageTransitionConfigurationTests
{
    [Fact(Skip = "Stub: implement in 04-06")]
    public void BuildStateMachine_WithTransitions_CanPermitConfiguredTransitions()
    {
        // TODO: Build Stateless StateMachine with stages [New, Qualified, ClosedWon],
        // transitions [New->Qualified, Qualified->ClosedWon],
        // verify machine.CanFire(New->Qualified)=true, machine.CanFire(New->ClosedWon)=false
    }

    [Fact(Skip = "Stub: implement in 04-06")]
    public void BuildStateMachine_NoTransitionsConfigured_CannotFireAnyTransition()
    {
        // TODO: Build StateMachine with 2 stages, no transitions added,
        // verify machine.CanFire() returns false for any target
    }

    [Fact(Skip = "Stub: implement in 04-06")]
    public void StageType_ClosedWon_MarkedAsTerminalState()
    {
        // TODO: Create PipelineStage with StageType.ClosedWon, verify IsTerminal property returns true
    }
}
```
  </action>
  <verify>
    <automated>dotnet test /home/manoj/projects/sandbox/IronMonkey/IronMonkey.Tests --filter "FullyQualifiedName~KanbanBoardTests|FullyQualifiedName~TaskTests|FullyQualifiedName~LeadRoutingTests|FullyQualifiedName~WorkflowRuleTests|FullyQualifiedName~StateTransitionTests|FullyQualifiedName~StageTransitionConfigurationTests" 2>&1 | tail -5</automated>
  </verify>
  <done>All 6 test files exist. Test run exits 0 with all stubs skipped (0 failed, N skipped).</done>
  <acceptance_criteria>
    - `IronMonkey.Tests/Integration/KanbanBoardTests.cs` exists with `[Collection("Integration")]`
    - `IronMonkey.Tests/Integration/TaskTests.cs` exists with `[Collection("Integration")]`
    - `IronMonkey.Tests/Integration/LeadRoutingTests.cs` exists with `[Collection("Integration")]`
    - `IronMonkey.Tests/Integration/WorkflowRuleTests.cs` exists with `[Collection("Integration")]`
    - `IronMonkey.Tests/Integration/StateTransitionTests.cs` exists with `[Collection("Integration")]`
    - `IronMonkey.Tests/Unit/StageTransitionConfigurationTests.cs` exists with `namespace IronMonkey.Tests.Unit`
    - `dotnet test IronMonkey.Tests --filter "FullyQualifiedName~KanbanBoardTests"` exits 0 (all skipped)
    - `dotnet build IronMonkey.sln` exits 0
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `dotnet build IronMonkey.sln` exits 0
- `dotnet test IronMonkey.Tests --filter "FullyQualifiedName~KanbanBoardTests|FullyQualifiedName~TaskTests|FullyQualifiedName~LeadRoutingTests|FullyQualifiedName~WorkflowRuleTests|FullyQualifiedName~StateTransitionTests|FullyQualifiedName~StageTransitionConfigurationTests"` exits 0 (all stubs skipped)
- Both .csproj files contain Stateless 5.20.1 and RulesEngine 5.1.2
</verification>

<success_criteria>
6 test stub files exist. Build passes. Test filter command exits 0 with 0 failures.
</success_criteria>

<output>
After completion, create `.planning/phases/04-pipeline-workflow-engine/04-01-SUMMARY.md`
</output>
