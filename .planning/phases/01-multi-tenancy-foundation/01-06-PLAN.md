---
phase: 01-multi-tenancy-foundation
plan: 06
type: execute
wave: 5
depends_on:
  - 01-05
files_modified:
  - IronMonkey.Tests/Integration/TenantIsolationTests.cs
  - IronMonkey.Tests/Integration/TenantProvisioningTests.cs
  - IronMonkey.Tests/Integration/CrossTenantSecurityTests.cs
  - IronMonkey.Tests/Unit/HangfireJobTenantTests.cs
autonomous: false
requirements:
  - TNCY-01
  - TNCY-02

must_haves:
  truths:
    - "All previously-skipped integration tests now pass (0 skipped in integration suite)"
    - "TenantIsolationTests: Tenant A context cannot retrieve Tenant B data"
    - "CrossTenantSecurityTests: JWT without tenant_id claim is rejected"
    - "TenantProvisioningTests: full signup-to-provision flow creates real database with schema and seed"
    - "HangfireJobTenantTests: explicit TenantId parameter validated in all job tests"
    - "Full test suite completes with 0 failures"
  artifacts:
    - path: "IronMonkey.Tests/Integration/TenantIsolationTests.cs"
      provides: "TNCY-02 data isolation verification — all stubs implemented"
    - path: "IronMonkey.Tests/Integration/CrossTenantSecurityTests.cs"
      provides: "TNCY-02 API security verification — all stubs implemented"
    - path: "IronMonkey.Tests/Integration/TenantProvisioningTests.cs"
      provides: "TNCY-01 end-to-end provisioning — all stubs implemented"
    - path: "IronMonkey.Tests/Unit/HangfireJobTenantTests.cs"
      provides: "TNCY-02 Hangfire isolation — all stubs implemented"
  key_links:
    - from: "IronMonkey.Tests"
      to: "IronMonkey.ApiService (all provisioning + auth endpoints)"
      via: "WebApplicationFactory or direct service instantiation with TestContainers PostgreSQL"
      pattern: "PostgreSqlFixture.*ConnectionString"
---

<objective>
Complete all remaining test implementations and achieve a fully green test suite. This is the phase gate: all Phase 1 requirements are verified by automated tests before proceeding to Phase 2.

Purpose: Every test stub created in Plan 01 and partially filled in Plans 02-05 must be fully implemented. This plan closes any remaining skip/incomplete tests and produces a definitive green state that satisfies the Roadmap success criteria for Phase 1.
Output: All integration and unit tests passing. Zero skipped tests in the Phase 1 test suite.
</objective>

<execution_context>
@/home/manoj/.claude/get-shit-done/workflows/execute-plan.md
@/home/manoj/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-multi-tenancy-foundation/01-VALIDATION.md
@.planning/phases/01-multi-tenancy-foundation/01-05-SUMMARY.md
@IronMonkey.Tests/Fixtures/PostgreSqlFixture.cs
@IronMonkey.Tests/Integration/TenantProvisioningTests.cs
@IronMonkey.Tests/Integration/TenantIsolationTests.cs
@IronMonkey.Tests/Integration/CrossTenantSecurityTests.cs
@IronMonkey.Tests/Unit/HangfireJobTenantTests.cs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement all remaining test stubs — achieve zero skipped</name>
  <files>
    IronMonkey.Tests/Integration/TenantIsolationTests.cs,
    IronMonkey.Tests/Integration/TenantProvisioningTests.cs,
    IronMonkey.Tests/Integration/CrossTenantSecurityTests.cs,
    IronMonkey.Tests/Unit/HangfireJobTenantTests.cs
  </files>
  <behavior>
    - TenantIsolationTests: 2 tests — cross-tenant data isolation via EF global query filters
    - TenantProvisioningTests: 4 tests — signup, approval, rejection, provisioning
    - CrossTenantSecurityTests: 2 tests — JWT enforcement at API layer
    - HangfireJobTenantTests: 2 tests — explicit TenantId in jobs
  </behavior>
  <action>
    For each test file, read its current state. For any test that still has `[Fact(Skip=...)]`, implement it.

    **Approach for each test class:**

    **TenantIsolationTests** (if not already complete from Plan 02):
    - Use `TenantDbContextFactory` directly (no HTTP layer needed)
    - Create two PostgreSQL databases from `PostgreSqlFixture.ConnectionString` by varying the DB name
    - For each: call `db.Database.EnsureCreatedAsync()` or `MigrateAsync()`
    - Seed one User with unique tenantId in each DB
    - Query Tenant A context: assert User count = 1, the user belongs to Tenant A
    - Query Tenant B context: assert User count = 1, the user belongs to Tenant B
    - Cross-query: Tenant A context returns empty when querying for Tenant B's userId

    **TenantProvisioningTests** (if not already complete from Plan 04):
    - Use `ITenantProvisioningService` directly (inject with real CentralDbContext against test PostgreSQL)
    - Override CentralDb connection string to point to test container
    - Test full flow: create SignupRequest → Approve → Provision
    - After Provision: verify `Tenant.IsProvisioned == true` and `DatabaseConnectionString != null`
    - Connect to the new tenant DB: verify schema exists (`db.Database.CanConnectAsync()` returns true)
    - Verify seed: at least one User exists in tenant DB

    **CrossTenantSecurityTests** (if not already complete from Plan 03):
    - Test 1: Build a valid JWT for Tenant A (using `Jwt.GenerateToken` directly with TenantId=tenantAId)
              Create a mock `IHttpContextAccessor` with this JWT's claims
              Instantiate `TenantService` with mock accessor and a real CentralDbContext
              Call `TenantService.GetCurrentTenantId()` → assert returns tenantAId
    - Test 2: Build a JWT without `tenant_id` claim
              Instantiate `TenantService` with that JWT
              Call `TenantService.GetCurrentTenantId()` → assert throws `ApplicationException`

    **HangfireJobTenantTests** (if not already complete from Plan 05):
    - Test 1: Mock `ITenantRegistry` returning "host=test;database=test"
              Mock `ITenantDbContextFactory` returning an in-memory `TenantDbContext`
              Call `OutboxProcessingJob.ExecuteAsync(specificTenantId)`
              Assert: `_tenantRegistry.GetConnectionStringAsync` was called exactly once with `specificTenantId`
    - Test 2: Mock `ITenantRegistry` throwing `InvalidOperationException` for unknown tenant
              Call `OutboxProcessingJob.ExecuteAsync(unknownTenantId)`
              Assert: `InvalidOperationException` propagates (not swallowed)

    After implementing all tests, run the full suite and fix any failures before completing this task.
  </action>
  <verify>
    <automated>cd /home/manoj/projects/sandbox/IronMonkey && dotnet test IronMonkey.Tests/IronMonkey.Tests.csproj --verbosity normal 2>&1 | tail -20</automated>
  </verify>
  <done>dotnet test reports: 0 failed, 0 skipped, 10+ passed. All Phase 1 tests are green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 1 multi-tenancy foundation:
    - Plan 01: Test scaffold (xUnit + TestContainers)
    - Plan 02: PostgreSQL dual-context (CentralDbContext + TenantDbContext with global query filters)
    - Plan 03: JWT TenantId claim pipeline, ITenantService, LoginEndpoint
    - Plan 04: Signup → Approve → Provision workflow, TenantProvisioningService
    - Plan 05: Hangfire with explicit TenantId jobs, migration orchestration
    - Plan 06: All integration/unit tests passing
  </what-built>
  <how-to-verify>
    1. Start the API: `cd /home/manoj/projects/sandbox/IronMonkey && dotnet run --project IronMonkey.ApiService`
    2. Open Swagger UI at `https://localhost:{port}/swagger`
    3. Submit a signup request: `POST /auth/signup` with company name, email, password, phone, industry, size, address, billing contact
    4. Approve it: `POST /admin/signup/{id}/approve`
    5. Provision it: `POST /admin/tenants/{id}/provision`
    6. Verify a new PostgreSQL database was created (check pg_database or connection string in Tenants table)
    7. Login: `POST /auth/login` with the admin email + password — verify JWT response contains `TenantId` in decoded payload
    8. Visit `/hangfire` — verify it requires authentication
    9. Run full test suite: `dotnet test IronMonkey.Tests/IronMonkey.Tests.csproj` — all green
  </how-to-verify>
  <resume-signal>Type "approved" if Phase 1 is working end-to-end, or describe any failures</resume-signal>
</task>

</tasks>

<verification>
- `dotnet test IronMonkey.Tests/IronMonkey.Tests.csproj` exits with code 0 (all pass)
- No skipped tests in output
- TenantIsolationTests, CrossTenantSecurityTests, TenantProvisioningTests, HangfireJobTenantTests all pass
- Roadmap Phase 1 success criteria verified:
  1. New tenant signup provisions separate DB — confirmed by TenantProvisioningTests
  2. Tenant A cannot read Tenant B data — confirmed by TenantIsolationTests + CrossTenantSecurityTests
  3. All API requests resolve correct tenant from JWT — confirmed by CrossTenantSecurityTests
  4. Background jobs carry explicit TenantId — confirmed by HangfireJobTenantTests
</verification>

<success_criteria>
Zero failing tests. Zero skipped tests. Platform admin can perform end-to-end signup-to-provision flow. Tenant isolation verified at both data layer (EF query filters) and API layer (JWT claims). Hangfire jobs use explicit TenantId parameters throughout. Phase 1 is ready for verify-work.
</success_criteria>

<output>
After completion, create `.planning/phases/01-multi-tenancy-foundation/01-06-SUMMARY.md`
</output>
